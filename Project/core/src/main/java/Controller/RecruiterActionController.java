package Controller;

public class RecruiterActionController {

    /**
     * Moves the recruiter
     * Sets token if passed
     * @param feature The feature asked about
     * @return True if moved, else false
     */
    public boolean move(){
        return true;
    }
    
    /**
     * Asks the recruiter about crossed features
     * Sets token if passed
     * @param void
     * @return True if moved, else false
     */
    public boolean mindSlip(){
        // Check Valid Mindslip moves

        // Send valid moves to view (recieve player move)

        // Update board state
        
        return true;
    }

    /**
     * Asks the recruiter about crossed features
     * Sets token if passed
     * @param feature The feature asked about
     * @return True if crossed, else false
     */
    public int answer(){

        // Iterate through recruiter path, return feature cells without footstep

        // If no footsteps return false

        // else
        // Let recruiter pick cell through view

        // set token, update state
        // Return true
        return 1;
    }
    
}
